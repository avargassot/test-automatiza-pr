name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

# ------------------------------------------------------------
# Bloque agregado automáticamente por el script de automatización.
# Este contenido se añade al final del workflow existente.
# Puedes usar avargassot, test-automatiza-pr, main, .github/workflows/main.yml
# como variables, o agregar las tuyas en repoConfig.variables.
# ------------------------------------------------------------
env:
